import Tag from "../../Tag"

const Home = (props) => {
	// const { msgAlert, user } = props
	// console.log('props in home', props)

	return (
		<>
			<h2>Home Page</h2>
			<div>
				<Tag />
			</div>
		</>
	)
}

export default Home
